   * :attr:     :xmlref:`ForwardGI`
     :val:      0
     :desc:     **Don't forward** General Interrogation command to downstream outstations when received from upstream station (e.g. SCADA)

   * :(attr):
     :val:      1
     :desc:     **Forward** General Interrogation command to downstream outstations when received from upstream station (e.g. SCADA) (default value)


